export type inputBlogType = {
    name: string,
    description: string,
    websiteUrl: string
}

export type outputBlogType = {
    id: string,
    name: string,
    description: string,
    websiteUrl: string
}